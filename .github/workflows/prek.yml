name: Prek

on:
  pull_request:
  push:
    branches:
      - "main"

jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: jdx/mise-action@v4
        with:
          install: true
          install_args: "--locked"
      - uses: j178/prek-action@v2
