# Project Level Instructions

- When proposing implementation plans, ALWAYS output the full absolute file path in plain text (e.g. `/workspaces/tabularix/docs/implementation_plan.md`), because standard markdown links might not work in the user's environment.
- Every story implementation must include updating the project documentation if relevant, as well as the tests.
